# TeensyAudioSimpleWindSynth
A simple wind controller that uses a pressure sensor as a breath controller to control a Teensy Audio Board as the audio source, while using a MIDI keyboard to select notes.
